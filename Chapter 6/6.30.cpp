#include<iostream>
using namespace std;
opp(int a){
	 int r = 0, rem;
	  while(n != 0) {
        rem = n%10;
        r = r*10 + remainder;
        a /= 10;  }
    cout << "Reversed Number =  " << r;}
int main(){
    int a;
    cout << " PLZ Enter an integer: ";
    cin >> a;
  opp(a);}

