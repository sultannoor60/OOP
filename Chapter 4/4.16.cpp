#include<iostream>
using namespace std;
main(){
	float a,b,c,mul,tol;
	cout<<"\nEnter laps compeleted(-1 to exit): ";
	cin>>a;
	while(a !=-1){
	cout<<"Enter sponsorship rate: ";
	cin>>b;
	if(a>40){
		c=a-40;
		mul=(a*b)+c*(b/100*50)
		;tol=mul+tol;
		cout<<"Student contribution is: "<<mul<<endl;
	
	}else {
			mul=a*b;
		tol=mul+tol;
		cout<<"Student contribution is: "<<mul<<endl;
	}
		cout<<"\nEnter laps compeleted(-1 to exit): ";
	cin>>a;
}
	
	cout<<"Total fund raised: "<<tol<<endl;
}
